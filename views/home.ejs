<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>0gram - Instagram Alternative</title>
    <style>
        :root {
            --bg-color: #121212;
            --text-color: #f5f5f5;
            --primary-color: #0095f6;
            --border-color: #363636;
            --card-bg: #1e1e1e;
        }

        @media (prefers-color-scheme: light) {
            :root {
                --bg-color: #fafafa;
                --text-color: #262626;
                --primary-color: #0095f6;
                --border-color: #dbdbdb;
                --card-bg: #ffffff;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: all 0.3s ease;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .container {
            max-width: 935px;
            margin: 0 auto;
            padding: 40px 20px;
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .logo {
            font-size: 3rem;
            font-weight: 300;
            margin-bottom: 20px;
            color: var(--primary-color);
        }

        .tagline {
            font-size: 1.2rem;
            margin-bottom: 40px;
            max-width: 600px;
            line-height: 1.6;
        }

        .search-box {
            width: 100%;
            max-width: 500px;
            margin-bottom: 30px;
        }

        .search-input {
            width: 100%;
            padding: 12px 20px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            font-size: 1rem;
            background-color: var(--card-bg);
            color: var(--text-color);
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        footer {
            text-align: center;
            padding: 20px;
            border-top: 1px solid var(--border-color);
            margin-top: auto;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="logo">0gram</h1>
        <p class="tagline">A minimal, privacy-focused alternative frontend for Instagram. View profiles and content
            without tracking or distractions.</p>

        <div class="search-box">
            <form action="/" method="GET">
                <input type="text" name="username" class="search-input" placeholder="Enter Instagram username..."
                    required>
            </form>
        </div>
    </div>

    <footer>
        <p>0gram is not affiliated with Instagram or Meta.</p>
    </footer>
</body>

</html>