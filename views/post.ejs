<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.png" />
    <title>0gram - Post</title>
    <style>
      :root {
        --bg-color: #121212;
        --text-color: #f5f5f5;
        --primary-color: #0095f6;
        --border-color: #363636;
        --card-bg: #1e1e1e;
        --secondary-text: #a8a8a8;
        --hover-bg: #2a2a2a;
      }

      @media (prefers-color-scheme: light) {
        :root {
          --bg-color: #fafafa;
          --text-color: #262626;
          --primary-color: #0095f6;
          --border-color: #dbdbdb;
          --card-bg: #ffffff;
          --secondary-text: #8e8e8e;
          --hover-bg: #f8f8f8;
        }
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family:
          -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica,
          Arial, sans-serif;
      }

      body {
        background-color: var(--bg-color);
        color: var(--text-color);
        transition: all 0.3s ease;
        min-height: 100vh;
        line-height: 1.4;
      }

      .header {
        background-color: var(--card-bg);
        border-bottom: 1px solid var(--border-color);
        padding: 16px 20px;
        position: sticky;
        top: 0;
        z-index: 100;
      }

      .header-content {
        max-width: 935px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .logo {
        font-size: 1.5rem;
        font-weight: 300;
        color: var(--primary-color);
        text-decoration: none;
      }

      .back-link {
        color: var(--text-color);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 12px;
        border-radius: 6px;
        transition: background-color 0.2s;
      }

      .back-link:hover {
        background-color: var(--hover-bg);
      }

      .container {
        max-width: 615px;
        margin: 0 auto;
        padding: 20px;
      }

      .post-card {
        background-color: var(--card-bg);
        border: 1px solid var(--border-color);
        border-radius: 8px;
        overflow: hidden;
        margin-bottom: 20px;
      }

      .post-header {
        padding: 16px;
        display: flex;
        align-items: center;
        gap: 12px;
        border-bottom: 1px solid var(--border-color);
      }

      .profile-pic {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        object-fit: cover;
      }

      .user-info {
        flex: 1;
      }

      .username {
        font-weight: 600;
        color: var(--text-color);
        text-decoration: none;
      }

      .username:hover {
        text-decoration: underline;
      }

      .verified-badge {
        color: var(--primary-color);
        margin-left: 4px;
      }

      .post-content {
        padding: 16px;
      }

      .caption {
        margin-bottom: 16px;
        white-space: pre-wrap;
        word-wrap: break-word;
      }

      .caption-username {
        font-weight: 600;
        margin-right: 8px;
      }

      .post-stats {
        padding: 12px 16px;
        border-top: 1px solid var(--border-color);
        border-bottom: 1px solid var(--border-color);
        color: var(--secondary-text);
        font-size: 14px;
      }

      .comments-section {
        background-color: var(--card-bg);
        border: 1px solid var(--border-color);
        border-radius: 8px;
        overflow: hidden;
      }

      .comments-header {
        padding: 12px 16px;
        border-bottom: 1px solid var(--border-color);
        font-weight: 600;
        background-color: var(--hover-bg);
      }

      .comment {
        padding: 12px 16px;
        border-bottom: 1px solid var(--border-color);
        transition: background-color 0.2s;
      }

      .comment:last-child {
        border-bottom: none;
      }

      .comment:hover {
        background-color: var(--hover-bg);
      }

      .comment-header {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 6px;
      }

      .comment-profile-pic {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        object-fit: cover;
        flex-shrink: 0;
      }

      .comment-user {
        font-weight: 600;
        color: var(--text-color);
        text-decoration: none;
      }

      .comment-user:hover {
        text-decoration: underline;
      }

      .comment-time {
        color: var(--secondary-text);
        font-size: 12px;
        margin-left: auto;
      }

      .comment-text {
        margin-left: 40px;
        white-space: pre-wrap;
        word-wrap: break-word;
        margin-bottom: 6px;
      }

      .comment-actions {
        margin-left: 40px;
        display: flex;
        align-items: center;
        gap: 16px;
        font-size: 12px;
        color: var(--secondary-text);
      }

      .like-count {
        font-weight: 600;
      }

      .child-comments {
        margin-left: 40px;
        margin-top: 8px;
        padding-left: 12px;
        border-left: 2px solid var(--border-color);
      }

      .child-comment {
        padding: 6px 0;
        border-bottom: none;
      }

      .child-comment:last-child {
        padding-bottom: 0;
      }

      .child-comment .comment-header {
        margin-bottom: 4px;
        gap: 6px;
      }

      .child-comment .comment-profile-pic {
        width: 24px;
        height: 24px;
      }

      .child-comment .comment-text {
        margin-left: 30px;
        margin-bottom: 0;
      }

      .load-more {
        color: var(--secondary-text);
        font-size: 12px;
        cursor: pointer;
        margin: 0;
      }

      .load-more:hover {
        color: var(--text-color);
      }

      .no-comments {
        padding: 32px 16px;
        text-align: center;
        color: var(--secondary-text);
      }

      .owner-badge {
        background-color: var(--primary-color);
        color: white;
        font-size: 10px;
        padding: 2px 6px;
        border-radius: 4px;
        margin-left: 8px;
      }

      @media (max-width: 768px) {
        .container {
          padding: 20px 16px;
        }

        .header-content {
          padding: 0 16px;
        }
      }
    </style>
  </head>

  <body>
    <div class="header">
      <div class="header-content">
        <a href="/" class="logo">0gram</a>
        <a href="/" class="back-link"> ← Back to search </a>
      </div>
    </div>

    <div class="container">
      <% if (comments && comments.caption) { %>
      <div class="post-card">
        <div class="post-header">
          <img
            src="/api/image?url=<%= encodeURIComponent(comments.caption.user.profile_pic_url) %>"
            alt="<%= comments.caption.user.full_name %>"
            class="profile-pic"
          />
          <div class="user-info">
            <a href="/<%= comments.caption.user.username %>" class="username">
              <%= comments.caption.user.full_name %> <% if
              (comments.caption.user.is_verified) { %>
              <span class="verified-badge">✓</span>
              <% } %>
            </a>
            <div style="font-size: 12px; color: var(--secondary-text)">
              @<%= comments.caption.user.username %>
            </div>
          </div>
        </div>

        <% if (comments.caption.text) { %>
        <div class="post-content">
          <div class="caption">
            <span class="caption-username"
              ><%= comments.caption.user.full_name %></span
            ><%= comments.caption.text %>
          </div>
        </div>
        <% } %>

        <div class="post-stats">
          <%= comments.comment_count %> comment<%= comments.comment_count !== 1
          ? 's' : '' %>
        </div>
      </div>
      <% } %>

      <div class="comments-section">
        <div class="comments-header">Comments</div>

        <% if (comments && comments.comments && comments.comments.length > 0) {
        %>
        <% comments.comments.forEach(function(comment) { %>
        <div class="comment">
          <div class="comment-header">
            <img
              src="/api/image?url=<%= encodeURIComponent(comment.user.profile_pic_url) %>"
              alt="<%= comment.user.full_name %>"
              class="comment-profile-pic"
            />
            <a href="/<%= comment.user.username %>" class="comment-user">
              <%= comment.user.full_name %> <% if (comment.user.is_verified) {
              %>
              <span class="verified-badge">✓</span>
              <% } %>
            </a>
            <% if (comment.is_created_by_media_owner) { %>
            <span class="owner-badge">AUTHOR</span>
            <% } %>
            <span class="comment-time">
              <%= new Date(comment.created_at * 1000).toLocaleDateString() %>
            </span>
          </div>

          <div class="comment-text"><%= comment.text %></div>

          <div class="comment-actions">
            <% if (comment.comment_like_count > 0) { %>
            <span class="like-count"
              ><%= comment.comment_like_count %>
              like<%=
              comment.comment_like_count !== 1 ? 's' : '' %></span
            >
            <% } %>
            <% if (comment.child_comment_count > 0) { %>
            <span class="load-more"
              >View
              <%= comment.child_comment_count %>
              repl<%=
              comment.child_comment_count !== 1 ? 'ies' : 'y' %></span
            >
            <% } %>
          </div>

          <% if (comment.preview_child_comments &&
          comment.preview_child_comments.length > 0) { %>
          <div class="child-comments">
            <% comment.preview_child_comments.forEach(function(childComment) {
            %>
            <div class="child-comment">
              <div class="comment-header">
                <img
                  src="/api/image?url=<%= encodeURIComponent(childComment.user.profile_pic_url) %>"
                  alt="<%= childComment.user.full_name %>"
                  class="comment-profile-pic"
                />
                <a
                  href="/<%= childComment.user.username %>"
                  class="comment-user"
                >
                  <%= childComment.user.full_name %>
                </a>
                <span class="comment-time">
                  <%= new Date(childComment.created_at *
                  1000).toLocaleDateString() %>
                </span>
              </div>
              <div class="comment-text"><%= childComment.text %></div>
            </div>
            <% }); %>
          </div>
          <% } %>
        </div>
        <% }); %>
        <% } else { %>
        <div class="no-comments">No comments yet</div>
        <% } %>
      </div>
    </div>
  </body>
</html>
